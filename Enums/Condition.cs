namespace SecondHandMarket.Enums;

/// <summary>
/// Represents the physical condition of a listed item.
/// </summary>
public enum Condition
{
    New = 1,
    LikeNew = 2,
    Good = 3,
    Fair = 4
}